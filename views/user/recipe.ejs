<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            font-family: 'Georgia', serif;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #f9f5f0;
        }
        
        .recipe-card {
            width: 6in;
            height: 4in;
            background-color: #fff9f5;
            border: 1px solid #e8d5c5;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            padding: 20px;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            position: relative;
            overflow: hidden;
        }
        
        .recipe-header {
            display: flex;
            gap: 15px;
            margin-bottom: 10px;
        }
        
        .recipe-photo {
            width: 150px;
            height: 120px;
            background-color: #f3e9e1;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 4px;
            overflow: hidden;
            flex-shrink: 0;
        }
        
        .recipe-photo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .recipe-meta {
            flex-grow: 1;
        }
        
        .recipe-title {
            margin: 0 0 5px 0;
            font-size: 22px;
            color: #5c3a21;
            font-weight: bold;
        }
        
        .divider {
            border-top: 2px solid #e8d5c5;
            margin: 10px 0;
            width: 100%;
        }
        
        h2 {
            margin: 0 0 8px 0;
            font-size: 16px;
            color: #7a5c44;
            border-bottom: 1px solid #e8d5c5;
            padding-bottom: 3px;
        }
        
        .recipe-content {
            display: flex;
            gap: 15px;
            flex-grow: 1;
            overflow: hidden;
        }
        
        .ingredients {
            flex: 1;
            overflow-y: auto;
            padding-right: 10px;
        }
        
        .ingredients-list {
            list-style-type: none;
            padding-left: 0;
            margin: 0;
        }
        
        .directions {
            flex: 1;
            overflow-y: auto;
            padding-left: 10px;
            border-left: 1px solid #e8d5c5;
        }
        
        .recipe-text {
            font-family: 'Georgia', serif;
            font-size: 13px;
            line-height: 1.4;
            color: #5c3a21;
            white-space: pre-wrap;
            margin: 0;
        }
        
        .back-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            background: rgba(255,255,255,0.8);
            border: none;
            padding: 2px 5px;
            font-size: 12px;
        }
    </style>
  </head>
  <body>
    <div class="recipe-card">
      <button class="back-btn" onclick="window.history.back()">← Back</button>
      <h1 class="recipe-title"><%= recipe.title %></h1>
      
      <div class="recipe-header">
          <div class="recipe-photo">
              <img src="<%= recipe.imageUrl || 'https://via.placeholder.com/150x120?text=Recipe+Image' %>" alt="Recipe Photo" />
          </div>
          <div class="recipe-meta">
              <!-- Additional metadata can go here if needed -->
          </div>
      </div>
      
      <div class="divider"></div>
      
      <div class="recipe-content">
          <div class="ingredients">
              <h2>INGREDIENTS</h2>
              <p class="recipe-text"><%= recipe.ingredients %></p>
          </div>
          <div class="directions">
              <h2>DIRECTIONS</h2>
              <p class="recipe-text"><%= recipe.instructions %></p>
          </div>
      </div>
    </div>
  </body>
</html>